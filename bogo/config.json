{
  "ErrorMap": {
    ":BAD_SIGNATURE:": [
      "zssl:BadSignature"
    ],
    ":WRONG_SIGNATURE_TYPE:": [
      "zssl:BadSignature",
      "zssl:BadCertificate"
    ],
    ":DIGEST_CHECK_FAILED:": [
      "zssl:DecryptError"
    ],
    ":DECRYPTION_FAILED_OR_BAD_RECORD_MAC:": [
      "zssl:AuthenticationFailed",
      "zssl:BadInnerPlaintext"
    ],
    ":BAD_DECRYPT:": [
      "zssl:AuthenticationFailed"
    ],
    ":UNEXPECTED_MESSAGE:": [
      "zssl:UnexpectedMessage"
    ],
    ":UNEXPECTED_RECORD:": [
      "zssl:UnexpectedMessage"
    ],
    ":EXCESS_HANDSHAKE_DATA:": [
      "zssl:UnexpectedMessage",
      "zssl:MalformedMessage"
    ],
    ":BAD_ALERT:": [
      "zssl:PeerAlert",
      "zssl:MalformedAlert"
    ],
    ":DECODE_ERROR:": [
      "zssl:MalformedMessage",
      "zssl:Truncated",
      "zssl:TrailingBytes",
      "zssl:BadServerHello",
      "zssl:UnexpectedMessage",
      "zssl:BadCertificate",
      "zssl:BadSignature",
      "zssl:IllegalKeyUpdate",
      "zssl:MalformedExtension"
    ],
    ":UNSUPPORTED_PROTOCOL:": [
      "zssl:BadServerHello",
      "zssl:UnsupportedLegacyVersion"
    ],
    ":WRONG_VERSION_NUMBER:": [
      "zssl:UnsupportedLegacyVersion"
    ],
    ":UNEXPECTED_EXTENSION:": [
      "zssl:BadServerHello",
      "zssl:UnexpectedMessage",
      "zssl:BadAlpn"
    ],
    ":ERROR_PARSING_EXTENSION:": [
      "zssl:MalformedExtension",
      "zssl:BadServerHello",
      "zssl:UnexpectedMessage"
    ],
    ":BAD_ECPOINT:": [
      "zssl:BadServerHello",
      "zssl:IdentityElement"
    ],
    ":WRONG_CURVE:": [
      "zssl:HandshakeFailure",
      "zssl:BadServerHello"
    ],
    ":WRONG_CIPHER_RETURNED:": [
      "zssl:BadServerHello"
    ],
    ":SERVER_ECHOED_INVALID_SESSION_ID:": [
      "zssl:BadServerHello"
    ],
    ":HANDSHAKE_FAILURE_ON_CLIENT_HELLO:": [
      "zssl:HandshakeFailure"
    ],
    ":NO_SHARED_CIPHER:": [
      "zssl:HandshakeFailure"
    ],
    ":NO_SHARED_GROUP:": [
      "zssl:HandshakeFailure"
    ],
    ":NO_APPLICATION_PROTOCOL:": [
      "zssl:NoApplicationProtocol",
      "zssl:BadAlpn"
    ],
    ":INVALID_ALPN_PROTOCOL:": [
      "zssl:BadAlpn"
    ],
    ":DATA_LENGTH_TOO_LONG:": [
      "zssl:RecordOverflow"
    ],
    ":RECORD_TOO_LARGE:": [
      "zssl:RecordOverflow"
    ],
    ":EXCESSIVE_MESSAGE_SIZE:": [
      "zssl:BufferOverflow"
    ],
    ":KEY_UPDATE_NOT_ALLOWED:": [
      "zssl:IllegalKeyUpdate",
      "zssl:TooManyKeyUpdates"
    ],
    ":CANNOT_PARSE_LEAF_CERT:": [
      "zssl:BadCertificate"
    ],
    ":UNSUPPORTED_ALGORITHM:": [
      "zssl:BadCertificate",
      "zssl:HandshakeFailure"
    ],
    ":NO_COMMON_SIGNATURE_ALGORITHMS:": [
      "zssl:HandshakeFailure"
    ],
    ":PARSE_TLSEXT:": [
      "zssl:BadAlpn",
      "zssl:MalformedExtension"
    ],
    ":MISSING_KEY_SHARE:": [
      "zssl:BadServerHello"
    ],
    ":OLD_SESSION_PRF_HASH_MISMATCH:": [
      "zssl:BadServerHello"
    ],
    ":PSK_IDENTITY_NOT_FOUND:": [
      "zssl:BadServerHello"
    ],
    ":UNKNOWN_ALERT_TYPE:": [
      "zssl:MalformedAlert"
    ],
    ":INVALID_OUTER_RECORD_TYPE:": [
      "zssl:UnexpectedMessage"
    ],
    ":TOO_MANY_EMPTY_FRAGMENTS:": [
      "zssl:UnexpectedMessage"
    ],
    ":ENCRYPTED_LENGTH_TOO_LONG:": [
      "zssl:RecordOverflow"
    ],
    ":PEER_DID_NOT_RETURN_A_CERTIFICATE:": [
      "zssl:BadCertificate"
    ]
  },
  "DisabledTests": {
    "ALPNClient-EmptyProtocolName-TLS-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ALPNClient-EmptyProtocolName-TLS-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ALPNClient-EmptyProtocolName-TLS-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ALPNClient-RejectUnknown-TLS-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ALPNClient-RejectUnknown-TLS-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ALPNClient-RejectUnknown-TLS-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ALPNClient-TLS-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ALPNClient-TLS-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ALPNClient-TLS-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Agree-Digest-Default": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "AlertAfterChangeCipherSpec": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "AlternateEmptyRecordsAndTooManyWarningAlerts": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "AlternateEmptyRecordsAndWarningAlerts": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "AlternateTooManyEmptyRecordsAndWarningAlerts": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "AppDataAfterChangeCipherSpec": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "AppDataAfterChangeCipherSpec-Empty": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadCBCPadding": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadCBCPadding255": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadChangeCipherSpec-1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadChangeCipherSpec-2": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDHECurve": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-1-1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-1-2": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-1-3": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-1-4": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-2-1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-2-2": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-2-3": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-2-4": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-3-1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-3-2": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-3-3": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-3-4": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-4-1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-4-2": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-4-3": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadECDSA-4-4": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "BadFinished-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Basic-Client-NoTicket-TLS-Sync": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Basic-Client-NoTicket-TLS-Sync-PackHandshake": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Basic-Client-NoTicket-TLS-Sync-SplitHandshakeRecords": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Basic-Client-TLS-Sync": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Basic-Client-TLS-Sync-PackHandshake": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Basic-Client-TLS-Sync-SplitHandshakeRecords": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Basic-Server-ECDHE-ECDSA-TLS-Sync": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Basic-Server-ECDHE-ECDSA-TLS-Sync-PackHandshake": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Basic-Server-ECDHE-ECDSA-TLS-Sync-SplitHandshakeRecords": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CertificateCipherMismatch-ECDSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CertificateCipherMismatch-Ed25519": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CertificateCipherMismatch-PSS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CertificateCipherMismatch-RSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CheckClientCertificateTypes": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CheckECDSACurve-NotApplicable-TLS13": "The case pairs a P-256 leaf with a P-384-only signature_algorithms list; zssl signs with the key it was given and fails the handshake.",
    "Client-InvalidSignature-RSA-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-InvalidSignature-RSA-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-RejectJDK11DowngradeRandom": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-ShortSessionID": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-Sign-RSA-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-Sign-RSA-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-ECDSA_P256_SHA256-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-ECDSA_P384_SHA384-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-ECDSA_P521_SHA512-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-ECDSA_SHA1-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-Ed25519-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-Ed25519-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-ML-DSA-44-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-ML-DSA-44-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-ML-DSA-65-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-ML-DSA-65-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-ML-DSA-87-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-ML-DSA-87-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-RSA-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-RSA-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-RSA_PKCS1_SHA1-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-RSA_PKCS1_SHA1-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-RSA_PKCS1_SHA256-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-RSA_PKCS1_SHA256-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-RSA_PKCS1_SHA256_LEGACY-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-RSA_PKCS1_SHA256_LEGACY-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-RSA_PKCS1_SHA384-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-RSA_PKCS1_SHA384-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-RSA_PKCS1_SHA512-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-RSA_PKCS1_SHA512-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-RSA_PSS_SHA256-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-RSA_PSS_SHA256-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-RSA_PSS_SHA384-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-RSA_PSS_SHA384-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-SignDefault-RSA_PSS_SHA512-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-SignDefault-RSA_PSS_SHA512-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "Client-TooLongSessionID": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-Verify-RSA-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Client-Verify-RSA-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-ECDSA-Client-TLS-Sync": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-ECDSA-Client-TLS-Sync-PackHandshake": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-ECDSA-Client-TLS-Sync-SplitHandshakeRecords": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-ECDSA-Client-TLS13-TLS-Sync": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientAuth-ECDSA-Client-TLS13-TLS-Sync-PackHandshake": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientAuth-ECDSA-Client-TLS13-TLS-Sync-SplitHandshakeRecords": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientAuth-NoCertificate-Client-TLS-Sync": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-NoCertificate-Client-TLS-Sync-PackHandshake": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-NoCertificate-Client-TLS-Sync-SplitHandshakeRecords": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-NoCertificate-Client-TLS13-TLS-Sync": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientAuth-NoCertificate-Client-TLS13-TLS-Sync-PackHandshake": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientAuth-NoCertificate-Client-TLS13-TLS-Sync-SplitHandshakeRecords": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientAuth-NoFallback-ECDSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-NoFallback-RSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-NoFallback-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientAuth-RSA-Client-TLS-Sync": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-RSA-Client-TLS-Sync-PackHandshake": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-RSA-Client-TLS-Sync-SplitHandshakeRecords": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-RSA-Client-TLS13-TLS-Sync": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientAuth-RSA-Client-TLS13-TLS-Sync-PackHandshake": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientAuth-RSA-Client-TLS13-TLS-Sync-SplitHandshakeRecords": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientAuth-SignatureType": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ClientAuth-SignatureType-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ClientSkipCertificateVerify-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "CurveID-Resume-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveID-Resume-Client-TLS13": "The runner offers P-256 on the resumed leg; zssl holds x25519 alone (DESIGN.md §1).",
    "CurveTest-Client-X25519-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Client-X25519-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Client-X25519-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Invalid-LowOrderX25519Point-Client-X25519-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Invalid-LowOrderX25519Point-Client-X25519-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Invalid-LowOrderX25519Point-Client-X25519-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Invalid-PadKeyShare-Client-X25519-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Invalid-PadKeyShare-Client-X25519-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Invalid-PadKeyShare-Client-X25519-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Invalid-TruncateKeyShare-Client-X25519-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Invalid-TruncateKeyShare-Client-X25519-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-Invalid-TruncateKeyShare-Client-X25519-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-SetX25519HighBit-Client-X25519-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-SetX25519HighBit-Client-X25519-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "CurveTest-SetX25519HighBit-Client-X25519-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Downgrade-TLS1-Client-TLS": "A 1.3-only client refuses the version before §4.1.3's downgrade sentinel can matter.",
    "Downgrade-TLS11-Client-TLS": "A 1.3-only client refuses the version before §4.1.3's downgrade sentinel can matter.",
    "Downgrade-TLS12-Client-TLS": "A 1.3-only client refuses the version before §4.1.3's downgrade sentinel can matter.",
    "DuplicateExtensionClient-TLS-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "DuplicateExtensionClient-TLS-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "DuplicateExtensionClient-TLS-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "DuplicateExtensionClient-TLS-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "ECDSACurveMismatch-Verify-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ECDSAKeyUsage-Client-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ECDSAKeyUsage-Client-TLS13": "X.509 key usage and path validation are the embedder's (DESIGN.md §1).",
    "EMS-Forbidden-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "EchoTLS13CompatibilitySessionID": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Ed25519DefaultDisable-NoAccept": "Ed25519: outside the signature policy (DESIGN.md §1).",
    "EmptyCertificateList": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "EmptyExtensions-ServerHello-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "EmptyExtensions-ServerHello-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "EmptyExtensions-ServerHello-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "EmptySessionID": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "EncryptedExtensionsWithKeyShare-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "ExtendedMasterSecret-NoToNo-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ExtendedMasterSecret-NoToYes-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ExtendedMasterSecret-YesToNo-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ExtensionTrailingData-CertificateAuthorities-Client-TLS-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "ExtensionTrailingData-ServerName-Client-TLS-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ExtensionTrailingData-ServerName-Client-TLS-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ExtensionTrailingData-ServerName-Client-TLS-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ExtensionTrailingData-ServerName-Client-TLS-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "FragmentAcrossChangeCipherSpec-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "FragmentAcrossChangeCipherSpec-Client-Packed": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "FragmentAcrossChangeCipherSpec-Client-Resume": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "FragmentAcrossChangeCipherSpec-Client-Resume-Packed": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "GarbageCertificate-Client-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "GarbageCertificate-Client-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "GarbageCertificate-Client-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "GarbageCertificate-Client-TLS13": "OPEN GAP: we refuse this, with a different alert than BoringSSL sends. docs/BOGO.md, finding 8.",
    "HelloRetryRequest-CipherChange-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "HelloRetryRequest-Cookie-Curve-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "HelloRetryRequest-Cookie-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "HelloRetryRequest-DuplicateCookie-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "HelloRetryRequest-DuplicateCurve-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "HelloRetryRequest-Empty-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "HelloRetryRequest-EmptyCookie-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "HelloRetryRequest-NonResumableCipher-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "HelloRetryRequest-Unknown-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "HelloRetryRequestCurveMismatch-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "HelloRetryRequestVersionMismatch-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "InvalidCompressionMethod": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "KeyUpdate-Requested": "OPEN GAP: two post-handshake messages in one record are refused; legal and common. docs/BOGO.md, finding 1.",
    "LargePlaintext-TLS13-Padded-16384-1": "OPEN GAP: no §5.4 cap on the decrypted inner plaintext once padding is stripped. docs/BOGO.md, finding 3.",
    "LargePlaintext-TLS13-Padded-8193-8192": "OPEN GAP: no §5.4 cap on the decrypted inner plaintext once padding is stripped. docs/BOGO.md, finding 3.",
    "MaxCBCPadding": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client-TLS1-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client-TLS1-TLS11-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client-TLS1-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client-TLS11-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client-TLS11-TLS11-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client-TLS11-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client-TLS12-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client-TLS12-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client-TLS13-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client-TLS13-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client2-TLS13-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "MinimumVersion-Client2-TLS13-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "NoClientCertificate-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "NoClientCertificate-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "NoClientCertificate-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "NoClientCertificate-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "NoEd25519-TLS11-ClientAuth-Sign": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "NoExtendedMasterSecret-TLS1-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "NoExtendedMasterSecret-TLS11-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "NoExtendedMasterSecret-TLS12-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "NoSSL3-Client-Unsolicited": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "NoSharedCipher": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "NoSharedCipher-TLS13": "Both ends refuse the offer and the runner's alert reaches us first, so we report its alert and not our own handshake_failure.",
    "OmitExtensions-ServerHello-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "OmitExtensions-ServerHello-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "OmitExtensions-ServerHello-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "PAKE-NormalClient-PAKEInHRR": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "PartialFinishedWithServerHelloDone": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "PartialNewSessionTicketWithServerHelloDone": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "PartialServerHelloWithHelloRetryRequest": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "PointFormat-Client-Missing": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "PointFormat-Client-MissingUncompressed": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "PointFormat-Client-Tolerance": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "PointFormat-EncryptedExtensions-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "PointFormat-ServerHello-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "PostQuantumEnabledByDefaultInClients": "Post-quantum key exchange: zssl offers x25519 alone (DESIGN.md §1).",
    "RSAEphemeralKey": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantEncipherment-GotEnciphermentTLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantEncipherment-GotEnciphermentTLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantEncipherment-GotEnciphermentTLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantEncipherment-GotSignature-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantEncipherment-GotSignature-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantEncipherment-GotSignature-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantSignature-GotEncipherment-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantSignature-GotEncipherment-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantSignature-GotEncipherment-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantSignature-GotEncipherment-TLS13": "X.509 key usage and path validation are the embedder's (DESIGN.md §1).",
    "RSAKeyUsage-Client-WantSignature-GotSignature-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantSignature-GotSignature-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RSAKeyUsage-Client-WantSignature-GotSignature-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RejectEmptyCertificateAuthorities-Client-TLS-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "RejectPSSKeyType-Client-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RejectPSSKeyType-Client-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RejectPSSKeyType-Client-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Renegotiate-Client-Forbidden-1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Renegotiate-Client-NoIgnore": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "RenegotiationInfo-Forbidden-TLS13": "Renegotiation: out of scope permanently (DESIGN.md §1).",
    "Resume-Client-CipherMismatch": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-Mismatch-TLS1-TLS11-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-Mismatch-TLS1-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-Mismatch-TLS1-TLS13-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-Mismatch-TLS11-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-Mismatch-TLS11-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-Mismatch-TLS11-TLS13-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-Mismatch-TLS12-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-Mismatch-TLS12-TLS11-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-Mismatch-TLS12-TLS13-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-Mismatch-TLS13-TLS1-TLS": "The resumed leg offers 1.3 alone against a 1.2-only runner (DESIGN.md §1).",
    "Resume-Client-Mismatch-TLS13-TLS11-TLS": "The resumed leg offers 1.3 alone against a 1.2-only runner (DESIGN.md §1).",
    "Resume-Client-Mismatch-TLS13-TLS12-TLS": "The resumed leg offers 1.3 alone against a 1.2-only runner (DESIGN.md §1).",
    "Resume-Client-NoResume-TLS1-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS1-TLS11-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS1-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS1-TLS13-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS11-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS11-TLS11-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS11-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS11-TLS13-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS12-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS12-TLS11-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS12-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS12-TLS13-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-NoResume-TLS13-TLS1-TLS": "The resumed leg offers 1.3 alone against a 1.2-only runner (DESIGN.md §1).",
    "Resume-Client-NoResume-TLS13-TLS11-TLS": "The resumed leg offers 1.3 alone against a 1.2-only runner (DESIGN.md §1).",
    "Resume-Client-NoResume-TLS13-TLS12-TLS": "The resumed leg offers 1.3 alone against a 1.2-only runner (DESIGN.md §1).",
    "Resume-Client-TLS1-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-TLS11-TLS11-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Resume-Client-TLS12-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ResumeTLS12SessionID-TLS13": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SecondHelloRetryRequest-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "SecondServerHelloNoVersion-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "SecondServerHelloWrongVersion-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "SendBogusAlertType": "OPEN GAP: we refuse this, with a different alert than BoringSSL sends. docs/BOGO.md, finding 8.",
    "SendEmptyRecords": "OPEN GAP: no bound on empty records or on KeyUpdates within a session. docs/BOGO.md, finding 4.",
    "SendEmptySessionTicket-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SendEmptySessionTicket-TLS13": "OPEN GAP: we refuse this, with a different alert than BoringSSL sends. docs/BOGO.md, finding 8.",
    "SendInvalidRecordType": "OPEN GAP: we refuse this, with a different alert than BoringSSL sends. docs/BOGO.md, finding 8.",
    "SendSNIWarningAlert": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SendUnknownExtensionOnCertificate-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "SendUnsolicitedOCSPOnCertificate-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "SendUnsolicitedSCTOnCertificate-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "SendUserCanceledAlerts-TLS13": "OPEN GAP: every non-close_notify alert is fatal, including the warning-level user_canceled §6.1 leaves legal. docs/BOGO.md, finding 5.",
    "SendUserCanceledAlerts-TooMany-TLS13": "OPEN GAP: every non-close_notify alert is fatal, including the warning-level user_canceled §6.1 leaves legal. docs/BOGO.md, finding 5.",
    "SendWarningAlerts-Pass": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SendWarningAlerts-TLS13": "OPEN GAP: every non-close_notify alert is fatal, including the warning-level user_canceled §6.1 leaves legal. docs/BOGO.md, finding 5.",
    "SendWarningAlerts-TooMany": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerAuth-Enforced": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerAuth-SHA1-Fallback-ECDSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerHelloBogusCipher": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerNameExtensionClient-TLS-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerNameExtensionClient-TLS-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerNameExtensionClient-TLS-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerNameExtensionClientMismatch-TLS-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerNameExtensionClientMismatch-TLS-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerNameExtensionClientMismatch-TLS-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerNameExtensionClientMissing-TLS-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerNameExtensionClientMissing-TLS-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "ServerNameExtensionClientMissing-TLS-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SessionTicketsDisabled-Client-TLS-Sync": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SessionTicketsDisabled-Client-TLS-Sync-PackHandshake": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SessionTicketsDisabled-Client-TLS-Sync-SplitHandshakeRecords": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Shutdown-Runner-TLS-Sync-PackHandshake": "OPEN GAP: two post-handshake messages in one record are refused; legal and common. docs/BOGO.md, finding 1.",
    "Shutdown-Shim-ApplicationData-TLS-Sync": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Shutdown-Shim-ApplicationData-TLS-Sync-PackHandshake": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Shutdown-Shim-ApplicationData-TLS-Sync-SplitHandshakeRecords": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Shutdown-Shim-HelloRequest-Reject-TLS-Sync": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Shutdown-Shim-HelloRequest-Reject-TLS-Sync-PackHandshake": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Shutdown-Shim-HelloRequest-Reject-TLS-Sync-SplitHandshakeRecords": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Shutdown-Shim-TLS-Sync": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Shutdown-Shim-TLS-Sync-PackHandshake": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Shutdown-Shim-TLS-Sync-SplitHandshakeRecords": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SkipChangeCipherSpec-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SkipHelloRetryRequest-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "SkipNewSessionTicket": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SkipServerKeyExchange": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "StrayHelloRequest": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "StrayHelloRequest-Packed": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SupportedCurves-ServerHello-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "SupportedVersionSelection-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-ECH-Client-UnsolictedHRRExtension": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "TLS-TLS1-ECDHE_ECDSA_WITH_AES_128_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-ECDHE_ECDSA_WITH_AES_128_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-ECDHE_ECDSA_WITH_AES_256_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-ECDHE_ECDSA_WITH_AES_256_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-ECDHE_RSA_WITH_AES_128_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-ECDHE_RSA_WITH_AES_128_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-ECDHE_RSA_WITH_AES_256_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-ECDHE_RSA_WITH_AES_256_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-RSA_WITH_AES_128_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-RSA_WITH_AES_128_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-RSA_WITH_AES_256_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS1-RSA_WITH_AES_256_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-ECDHE_ECDSA_WITH_AES_128_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-ECDHE_ECDSA_WITH_AES_128_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-ECDHE_ECDSA_WITH_AES_256_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-ECDHE_ECDSA_WITH_AES_256_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-ECDHE_RSA_WITH_AES_128_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-ECDHE_RSA_WITH_AES_128_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-ECDHE_RSA_WITH_AES_256_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-ECDHE_RSA_WITH_AES_256_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-RSA_WITH_AES_128_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-RSA_WITH_AES_128_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-RSA_WITH_AES_256_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS11-RSA_WITH_AES_256_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_AES_128_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_AES_128_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_AES_128_CBC_SHA256-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_AES_128_CBC_SHA256-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_AES_128_GCM_SHA256-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_AES_128_GCM_SHA256-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_AES_256_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_AES_256_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_AES_256_GCM_SHA384-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_AES_256_GCM_SHA384-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_AES_128_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_AES_128_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_AES_128_CBC_SHA256-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_AES_128_CBC_SHA256-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_AES_128_GCM_SHA256-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_AES_128_GCM_SHA256-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_AES_256_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_AES_256_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_AES_256_GCM_SHA384-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_AES_256_GCM_SHA384-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-RSA_WITH_AES_128_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-RSA_WITH_AES_128_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-RSA_WITH_AES_128_GCM_SHA256-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-RSA_WITH_AES_128_GCM_SHA256-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-RSA_WITH_AES_256_CBC_SHA-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-RSA_WITH_AES_256_CBC_SHA-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-RSA_WITH_AES_256_GCM_SHA384-BadRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS-TLS12-RSA_WITH_AES_256_GCM_SHA384-LargeRecord": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS1-Client-ClientAuth-ECDSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS1-Client-ClientAuth-RSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS1-NoTicket-NoOffer": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS11-Client-ClientAuth-ECDSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS11-Client-ClientAuth-RSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS11-NoTicket-NoOffer": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS12-Client-ClientAuth-ECDSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS12-Client-ClientAuth-RSA": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS12-NoTicket-NoOffer": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS12SessionID-TLS13": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TLS13-1RTT-Client-TLS-Sync-PackHandshake": "OPEN GAP: two post-handshake messages in one record are refused; legal and common. docs/BOGO.md, finding 1.",
    "TLS13-Client-ClientAuth-ECDSA": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "TLS13-Client-ClientAuth-RSA": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "TLS13-Client-EmptyTicketFlags": "OPEN GAP: NewSessionTicket extension bodies are not checked for a minimal encoding. docs/BOGO.md, finding 7.",
    "TLS13-Client-NonminimalTicketFlags": "OPEN GAP: NewSessionTicket extension bodies are not checked for a minimal encoding. docs/BOGO.md, finding 7.",
    "TLS13-DuplicateTicketEarlyDataSupport": "0-RTT: deferred pending a replay analysis (DESIGN.md §1).",
    "TLS13-HRR-InvalidCompressionMethod": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "Ticket-Forbidden-TLS13": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TolerateServerNameAck-TLS-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TolerateServerNameAck-TLS-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TolerateServerNameAck-TLS-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TooManyKeyUpdates": "OPEN GAP: no bound on empty records or on KeyUpdates within a session. docs/BOGO.md, finding 4.",
    "TrailingDataWithFinished-Client-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingDataWithFinished-Client-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingDataWithFinished-Client-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingDataWithFinished-Resume-Client-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingDataWithFinished-Resume-Client-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingDataWithFinished-Resume-Client-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingMessageData-CertificateRequest-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingMessageData-NewSessionTicket-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingMessageData-ServerCertificate-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingMessageData-ServerFinished-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingMessageData-ServerHello-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingMessageData-ServerHelloDone-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingMessageData-ServerKeyExchange-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "TrailingMessageData-TLS13-CertificateRequest-TLS": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "TrailingMessageData-TLS13-EncryptedExtensions-TLS": "OPEN GAP: we refuse this, with a different alert than BoringSSL sends. docs/BOGO.md, finding 8.",
    "TrailingMessageData-TLS13-ServerCertificate-TLS": "OPEN GAP: we refuse this, with a different alert than BoringSSL sends. docs/BOGO.md, finding 8.",
    "TrailingMessageData-TLS13-ServerCertificateVerify-TLS": "OPEN GAP: we refuse this, with a different alert than BoringSSL sends. docs/BOGO.md, finding 8.",
    "TrustAnchors-Unsolicited-Certificate": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "TrustAnchors-Unsolicited-EncryptedExtensions": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "UnauthenticatedECDH": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "Unclean-Shutdown": "OPEN GAP: a close_notify retires the machine, so no close_notify can be sent back. docs/BOGO.md, finding 6.",
    "Unclean-Shutdown-Alert": "OPEN GAP: a close_notify retires the machine, so no close_notify can be sent back. docs/BOGO.md, finding 6.",
    "UnencryptedEncryptedExtensions": "OPEN GAP: we refuse this, with a different alert than BoringSSL sends. docs/BOGO.md, finding 8.",
    "UnknownCurve-HelloRetryRequest-TLS13": "HelloRetryRequest: the client holds one group, so both shapes are refused structurally (DESIGN.md §6, slice 4).",
    "UnknownExtension-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "UnknownExtension-Client-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "UnknownExtensionInCertificateRequest-TLS13": "Client certificates: out of scope permanently (DESIGN.md §1).",
    "UnofferedExtension-Client": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "UnofferedExtension-Client-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "UnsolicitedServerNameAck-TLS-TLS1": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "UnsolicitedServerNameAck-TLS-TLS11": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "UnsolicitedServerNameAck-TLS-TLS12": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "UnsolicitedServerNameAck-TLS-TLS13": "OPEN GAP: the client accepts an extension it never offered; §4.2 wants unsupported_extension. docs/BOGO.md, finding 2.",
    "Verify-ServerAuth-SignatureType": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "VersionNegotiation-Client-TLS13-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "VersionNegotiation-Client-TLS13-TLS11-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "VersionNegotiation-Client-TLS13-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "VersionNegotiation-Client-TLS13-TLS13-TLS": "The runner asserts an initial record version of 0x0301; §5.1 permits the 0x0303 we send.",
    "VersionNegotiation-Client2-TLS13-TLS1-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "VersionNegotiation-Client2-TLS13-TLS11-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "VersionNegotiation-Client2-TLS13-TLS12-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "VersionNegotiation-Client2-TLS13-TLS13-TLS": "The runner asserts an initial record version of 0x0301; §5.1 permits the 0x0303 we send.",
    "WrongMessageType-CertificateRequest-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "WrongMessageType-NewSessionTicket-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "WrongMessageType-ServerCertificate-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "WrongMessageType-ServerFinished-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "WrongMessageType-ServerHello-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "WrongMessageType-ServerHelloDone-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated.",
    "WrongMessageType-ServerKeyExchange-TLS": "TLS 1.0/1.1/1.2: 1.3-only by design (DESIGN.md §1) — the version is declined, never negotiated."
  }
}
